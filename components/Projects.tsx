import { ArrowUpRight, Github, Play } from "lucide-react";
import { projects } from "@/data/projects";
export default function Projects() {
 return <section id="projects" className="section shell"><div className="section-heading"><div><p className="eyebrow">01 / SELECTED WORK</p><h2>Ideas into <em>systems.</em></h2></div><p>Real-time platforms, applied machine learning,<br/>and experiments that became products.</p></div><div className="project-grid">{projects.map((project,i)=><article key={project.id} className={`project-card ${i<2?'featured':''}`}><div className="project-topline"><span>PROJECT / {String(i+1).padStart(2,'0')}</span><ArrowUpRight size={24}/></div><h3>{project.name.split(' – ')[0]}</h3>{project.name.includes(' – ')&&<p className="project-subtitle">{project.name.split(' – ')[1]}</p>}<p className="project-description">{project.description}</p><div className="tags">{project.tech.map(t=><span key={t}>{t}</span>)}</div><div className="project-links">{project.visitUrl&&<a href={project.visitUrl} target="_blank" rel="noopener noreferrer">{project.id===7?'Explore report':'Visit project'} <ArrowUpRight size={16}/></a>}{project.githubUrl&&<a href={project.githubUrl} target="_blank" rel="noopener noreferrer"><Github size={16}/> Code</a>}{project.videoUrl&&<a href={project.videoUrl} target="_blank" rel="noopener noreferrer"><Play size={16}/>{project.id===7?'Read PDF':'Watch demo'}</a>}</div></article>)}</div></section>;
}
